name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24


      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build backend image
        run: |
          docker build -t yousefabozahra/amazona-backend:latest ./backend


      - name: Push backend image
        run: |
          docker push yousefabozahra/amazona-backend:latest


      - name: Build frontend image
        run: |
          docker build -t yousefabozahra/amazona-frontend:latest ./frontend


      - name: Push frontend image
        run: |
          docker push yousefabozahra/amazona-frontend:latest